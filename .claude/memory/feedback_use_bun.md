---
name: Always use bun
description: User prefers bun over npm/yarn/pnpm for all package management and project creation
type: feedback
---

Always use `bun` instead of `npm`, `yarn`, or `pnpm` for package management, script running, and project initialization.

**Why:** User explicitly corrected an npm usage and stated "always use bun."

**How to apply:** Replace `npm`, `npx`, `yarn`, `pnpm` commands with `bun` equivalents. Use `bun create` for scaffolding, `bun install` for installing deps, `bun run` or `bun dev` for scripts.
